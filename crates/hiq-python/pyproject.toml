[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "hiq"
version = "1.0.1"
description = "Rust-native quantum compilation platform"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "Apache-2.0" }
authors = [
    { name = "Daniel Hinderink", email = "daniel@hal-contract.org" }
]
keywords = ["quantum", "compiler", "qasm", "circuit"]
urls = { Homepage = "https://github.com/hiq-lab/HIQ", Repository = "https://github.com/hiq-lab/HIQ", Issues = "https://github.com/hiq-lab/HIQ/issues" }
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Rust",
    "Programming Language :: Python :: Implementation :: CPython",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Physics",
]

[project.optional-dependencies]
dev = ["pytest>=7.0", "pytest-cov"]

[tool.maturin]
features = ["pyo3/extension-module"]
python-source = "python"
module-name = "hiq"
